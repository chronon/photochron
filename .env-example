# API configuration - your API endpoint base URL
PUBLIC_API_BASE=""

# Development configuration (for localhost testing only)
PUBLIC_USER_NAME="[YOUR_USERNAME]"

# Note:
# All other configuration (images, avatars, variants) now comes from the API response.
# The API endpoint format is: ${PUBLIC_API_BASE}/data/${username}/content.json
#
# Expected API response structure:
# {
#   "user": {
#     "name": "username",
#     "avatar": { "id": "cloudflare-image-id", "variant": "default" }
#   },
#   "config": {
#     "imageBase": "https://imagedelivery.net/account-hash",
#     "imageVariant": "default"
#   },
#   "images": [...]
# }
