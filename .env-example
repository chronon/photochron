# Cloudflare Images CDN configuration
PUBLIC_IMG_BASE="https://imagedelivery.net/[ACCOUNT_HASH]"
PUBLIC_IMG_VARIANT="default"

# API configuration
PUBLIC_API_BASE="https://api.chronon.dev"

# Development configuration (for localhost testing only)
PUBLIC_USER_NAME="[YOUR_USERNAME]"

# Note:
# - imgSource: ${PUBLIC_API_BASE}/data/${username}/images.json (derived from domain)
# - userAvatar: Uses {USERNAME}_AVATAR environment variable, falls back to convention-based
# - userName: extracted from domain (e.g., user.com -> user, admin.user.com -> user)
#
# Example avatar environment secrets (set via Cloudflare Dashboard or wrangler):
# USER1_AVATAR="abc12329-8231-4dac-e7d4-86e361718f00/default"
# USER2_AVATAR="abc12322-8827-4504-78c5-b76b377fdd00/default"
